/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using plain <img> tags exactly as in the original React app,
  // so no next/image domain configuration is required.
  reactStrictMode: true,
};

export default nextConfig;
